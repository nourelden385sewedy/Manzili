
@{
    ViewData["Title"] = "Edit Profile - Manzili";
    ViewData["ShowFooter"] = true;
}

@section Styles {
    <link rel="stylesheet" href="~/css/profile.css">
}

<main class="my-account-layout">
    <aside class="sidebar">
        <nav class="sidebar-nav">
            <ul>
                <li class="active"><a asp-page="/Profile"><i class="fas fa-user"></i> Profile</a></li>
                <li><a href="#"><i class="fas fa-box"></i> Orders</a></li>
                <li><a href="#"><i class="fas fa-heart"></i> Favorites</a></li>
                <li><a href="#"><i class="fas fa-credit-card"></i> Payment Methods</a></li>
                <li><a href="#"><i class="fas fa-bell"></i> Notifications</a></li>
                <li><a href="#"><i class="fas fa-cog"></i> Settings</a></li>
                <li class="logout"><a asp-page="/SignIn"><i class="fas fa-sign-out-alt"></i> Logout</a></li>
            </ul>
        </nav>
    </aside>

    <section class="main-content">
        <h1>My Account</h1>

        <form method="post" class="personal-info-card">
            <h2>Personal Information</h2>
            <div class="info-container">
                <div class="profile-section">
                    <div class="profile-pic-wrapper">
                        <img src="~/images/profile/1.png" alt="@Model.FullName" class="profile-pic">
                        <span class="edit-icon" style="cursor: default;"><i class="fas fa-pen"></i></span>
                    </div>
                    <div class="info-fields">
                        <div class="field-group">
                            <div class="info-field">
                                <span class="field-label">Full Name</span>
                                <input type="text" name="FullName" class="field-value" value="@Model.FullName" style="border: 1px solid #E5E7EB; padding: 0.5rem; border-radius: 6px; background: white;">
                            </div>
                            <div class="info-field">
                                <span class="field-label">Phone Number</span>
                                <input type="text" name="PhoneNumber" class="field-value" value="@Model.PhoneNumber" style="border: 1px solid #E5E7EB; padding: 0.5rem; border-radius: 6px; background: white;">
                            </div>
                        </div>
                        <div class="field-group">
                            <div class="info-field">
                                <span class="field-label">Email</span>
                                <input type="email" name="Email" class="field-value" value="@Model.Email" style="border: 1px solid #E5E7EB; padding: 0.5rem; border-radius: 6px; background: white;">
                            </div>
                            <div class="info-field">
                                <span class="field-label">Member Since</span>
                                <span class="field-value">@Model.MemberSince</span>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="edit-btn-wrapper">
                    <a asp-page="/Profile" class="edit-btn" style="background-color: #6B7280; text-decoration: none; text-align: center;">Cancel</a>
                    <button type="submit" class="edit-btn">Save Changes</button>
                </div>
            </div>
        </form>

        <div class="addresses-card">
            <div class="addresses-header">
                <h2>Saved Addresses</h2>
                <a href="#" class="add-new-address-link">Add New Address</a>
            </div>

            @foreach (var address in Model.Addresses)
            {
                <div class="address-item">
                    <div class="address-details">
                        <h3 class="address-name">@address.Name</h3>
                        <p class="address-text">@Html.Raw(address.AddressText)</p>
                        <div class="action-links">
                            <a href="#" class="edit-link">Edit</a>
                            @if (!address.IsDefault)
                            {
                                <a href="#" class="set-default-link">Set as Default</a>
                            }
                            <a href="#" class="delete-link">Delete</a>
                        </div>
                    </div>
                    @if (address.IsDefault)
                    {
                        <span class="default-badge">Default</span>
                    }
                </div>
            }
        </div>
    </section>
</main>

